{
    "name": "geoquery",
    "version": "1.0.1",
    "keywords": [
        "util",
        "functional",
        "server",
        "client",
        "browser",
        "geography",
        "places"
    ],
    "author": "Ndifreke Ekim",
    "contributors": [],
    "dependencies": {
        "dotenv": "^8.0.0",
        "graphql": "latest"
    },
    "devDependencies": {
        "nodemon": "^1.19.0",
        "babel-plugin-module-resolver": "^3.2.0",
        "@babel/core": "^7.2.2",
        "@babel/node": "^7.2.2",
        "@babel/cli": "^7.2.3",
        "@babel/preset-env": "^7.3.1",
        "eslint": "^5.16.0",
        "husky": "2.3.0",
        "jest": "^24.8.0",
        "coveralls": "^3.0.3"
    },
    "scripts": {
        "coveralls": "cat ./coverage/lcov.info | coveralls",
        "test": "jest __tests__/index.js --coverage",
        "clean": "rm -rf build",
        "build": "npm run clean && babel ./server -d build --copy-files",
        "demo": "npm run build && node build/example",
        "nodemon": "nodemon --exec babel-node server/example.js"
    }
}
